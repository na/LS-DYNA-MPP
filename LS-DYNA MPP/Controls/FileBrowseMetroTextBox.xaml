<UserControl x:Class="Predictive.Lsdyna.Mpp.Controls.FileBrowseMetroTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Height="Auto">
    <Grid x:Name="LayoutRoot">
        <TextBox x:Name="FilePath"
            Style="{StaticResource OpenFileMetroTextBox}" 
            Controls:TextBoxHelper.Watermark="{Binding Watermark}"
            Controls:TextBoxHelper.ButtonCommand="{Binding BrowseFile}"/>
    </Grid>
</UserControl>
